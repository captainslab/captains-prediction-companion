# Market Structure Analyst — {{market_id}}

Auto-built from Kalshi API. Nothing to do unless adding movement notes.

Board:
{{boardSummary}}

If desired, return JSON augmenting `marketStructure.notes` (array of strings). Otherwise skip.
