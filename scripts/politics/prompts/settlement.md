# Settlement Rules Analyst — {{market_id}}

Auto-built from Kalshi API. Augment only the `ambiguities` list. Do not invent rules.

Live rules text:
{{rules}}

Acting/Interim handling: {{actingInterim}}

Return JSON:
```json
{ "ambiguities": ["<one ambiguity per line, or empty>"] }
```
Scope: rules text only. No political prediction. No price talk.
